[project]
name = "jonnyzzz-loto"
version = "0.1.0"
description = "Russian Loto number announcer with TTS"
requires-python = ">=3.10"
dependencies = [
    "edge-tts>=6.1.0",
    "pygame>=2.5.0",
]

[project.scripts]
loto = "loto:main"
